RSpec.describe Activerecord::Tenant::Level::Security do
  it "has a version number" do
    expect(Activerecord::Tenant::Level::Security::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
